import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: { main: '#1f3a5f' },
    secondary: { main: '#d97a2b' },
  },
  shape: { borderRadius: 10 },
});
